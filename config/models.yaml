# OpenAI-compatible endpoints. Override base_url per role or via OPENAI_BASE_URL.
# Currently configured for DeepSeek API.
default_base_url: null

embedding:
  model: text-embedding-3-small
  temperature: 0.0

triage:
  model: deepseek-chat
  temperature: 0.1

structured_analysis:
  model: deepseek-chat
  temperature: 0.2

deep_review:
  model: deepseek-reasoner
  temperature: 0.2

math_analysis:
  model: deepseek-reasoner
  temperature: 0.1
